<section class="custom-banner">
  <div class="banner-container text-{{ section.settings.text_alignment }} align-{{ section.settings.vertical_alignment }}">
    <div class="banner-content banner-width-{{ section.settings.content_width }}">
      <h1>{{ section.settings.heading }}</h1>
      <p>{{ section.settings.subheading }}</p>
      <a href="{{ section.settings.button_link }}" class="btn">{{ section.settings.button_text }}</a>
    </div>
  </div>
</section>

<style>
  .custom-banner {
    position: relative;
    width: 100%;
    background: url('{{ section.settings.bg_image | image_url }}') no-repeat center center;
    background-size: cover;
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 60vh;
    padding: 2rem;
  }

  .banner-container {
    display: flex;
    justify-content: flex-start; /* Default Left */
    align-items: flex-start; /* Default Top */
    width: 100%;
    padding: 2rem;
  }

  /* Horizontal Text Alignment */
  .text-left { justify-content: flex-start; }
  .text-center { justify-content: center; }
  .text-right { justify-content: flex-end; }

  /* Vertical Text Alignment */
  .align-top { align-items: flex-start; }
  .align-middle { align-items: center; }
  .align-bottom { align-items: flex-end; }

  /* Content Box Width */
  .banner-width-small { max-width: 400px; }
  .banner-width-medium { max-width: 600px; }
  .banner-width-large { max-width: 800px; }

  .banner-content {
    padding: 2rem;
    border-radius: 10px;
    color: #fff; /* Ensures white text */
    text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.6); /* Improves readability */
  }

  .banner-content h1 {
    font-size: 2rem;
    font-weight: bold;
  }

  .banner-content p {
    font-size: 1.2rem;
    margin: 1rem 0;
  }

  .btn {
    background: #ffcc66;
    color: #000;
    padding: 10px 20px;
    text-decoration: none;
    border-radius: 5px;
    display: inline-block;
    font-weight: bold;
  }

  @media (max-width: 768px) {
    .custom-banner {
      min-height: 40vh;
      padding: 1rem;
    }

    .banner-content h1 {
      font-size: 1.5rem;
    }

    .banner-content p {
      font-size: 1rem;
    }

    .btn {
      padding: 8px 16px;
    }
  }
</style>
